<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Multi-Robot Shared Autonomy</title>
  <style>
    /* Reset & Base */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap');

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #0f0c29, #302b63, #24243e, #1c1c28);
      background-size: 400% 400%;
      animation: backgroundShift 10s ease infinite;
      color: white;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      transition: background 5s ease-in-out;
      overflow: hidden;
    }

    @keyframes backgroundShift {
      0% { background-position: 0% 50%; }
      25% { background-position: 50% 50%; }
      50% { background-position: 100% 50%; }
      75% { background-position: 50% 50%; }
      100% { background-position: 0% 50%; }
    }

    /* Beam Light Animation Overlay */
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: -50%;
      width: 200%;
      height: 100%;
      background: linear-gradient(
        120deg,
        rgba(255, 255, 255, 0) 40%,
        rgba(255, 255, 255, 0.15) 50%,
        rgba(255, 255, 255, 0) 60%
      );
      animation: beamMove 6s linear infinite;
      z-index: 0;
      pointer-events: none;
    }

    @keyframes beamMove {
      0% { transform: translateX(-50%); }
      100% { transform: translateX(50%); }
    }

    /* Make content stay above the beam */
    body > * {
      position: relative;
      z-index: 1;
    }

    a {
      color: inherit;
      text-decoration: none;
      transition: color 0.3s ease;
    }

    /* Header */
    header {
      background: transparent;
      backdrop-filter: blur(10px);
      padding: 20px;
    }

    nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
    }

    nav h2 {
      font-size: 2rem;
      font-weight: 700;
      color: #fff;
      letter-spacing: 1px;
    }

    nav ul {
      list-style: none;
      display: flex;
      gap: 30px;
    }

    nav ul li a {
      font-weight: 500;
      color: white;
      font-size: 1.1rem;
      transition: color 0.3s;
    }

    nav ul li a:hover {
      color: #ff1493;
    }

    /* Main Content */
    main {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 60px 20px;
      max-width: 800px;
      margin: 0 auto;
      animation: fadeInUp 1.5s ease-out;
    }

    @keyframes fadeInUp {
      0% {
        opacity: 0;
        transform: translateY(30px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    main h1 {
      font-size: 3rem;
      margin-bottom: 20px;
      font-weight: 800;
      color: white;
      letter-spacing: 1px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    main p {
      font-size: 1.2rem;
      margin-bottom: 40px;
      color: white;
    }

    /* Footer */
    footer {
      background: transparent;
      text-align: center;
      padding: 20px;
      font-size: 0.9rem;
      color: white;
      opacity: 0;
      animation: fadeInFooter 1s forwards;
    }

    @keyframes fadeInFooter {
      100% {
        opacity: 1;
      }
    }

    /* Responsive */
    @media (max-width: 600px) {
      nav {
        flex-direction: column;
        align-items: flex-start;
      }

      nav ul {
        flex-direction: column;
        gap: 10px;
        margin-top: 10px;
      }
    }

    /* Optional Robot Icon Styling */
    .robot-container {
      display: none;
    }

    .robot-icon {
      width: 100px;
      height: 100px;
      background: url('robot.png') no-repeat center center;
      background-size: contain;
    }
  </style>
</head>
<body>

  <!-- Optional Robot Animation -->
  <div class="robot-container">
    <div class="robot-icon"></div>
  </div>

  <!-- Header -->
  <header>
    <nav>
      <h2>Multi-Bot</h2>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/simulate">Simulation</a></li>
        <li><a href="#">About</a></li>
      </ul>
    </nav>
  </header>

  <!-- Main Content -->
  <main>
    <div>
      <h1>Welcome to the Multi-Bot</h1>
      <p>This website simulates how multiple robots request help from one human operator using a bidding system.</p>
    </div>
  </main>

  <!-- Footer -->
  <footer>
    <p>&copy; 2025 Multi-Robot Allocation of Assistance from a Shared Uncertain Operator Project</p>
  </footer>

</body>
</html>
